extends base

block content

  main.w3-row(style='padding-left:0;')
   .w3-container.w3-light-gray.w3-padding(style='width:100%;padding-left:0%')
    each tour in tours
      .w3-container.w3-margin-bottom.w3-margin-top(class='w3-col m6 l4',style='margin-left:0.0%;')
        .w3-card(style='width:100%')
          .card__header
            .card__picture
              
              img.card__picture-img(src=`img/tours/${tour.imageCover}`, alt=`${tour.name}`)
            h3.heading-tertirary
              span= tour.name

          .card__details
            h4.card__sub-heading= `${tour.difficulty} ${tour.duration}-day tour` 
            p.card__text= tour.summary 
            .card__data
              svg.card__icon
                use(xlink:href='/img/icons.svg#icon-map-pin')
              span= tour.startLocation.description
            .card__data
              svg.card__icon
                use(xlink:href='/img/icons.svg#icon-calendar')
              span= tour.startDates[0].toLocaleString('en-us',{month: 'long', year:'numeric'})
            .card__data
              svg.card__icon
                use(xlink:href='/img/icons.svg#icon-flag')
              span= `${tour.locations.length} stops` 
            .card__data
              svg.card__icon
                use(xlink:href='/img/icons.svg#icon-user')
              span= `${tour.maxGroupSize} people`

          .card__footer(style="margin-bottom:0%")
            p
              span.card__footer-value= `₦${tour.price*100}`
              | 
              span.card__footer-text per person
            p.card__ratings
              span.card__footer-value= tour.ratingsAverage
              | 
              span.card__footer-text= `rating (${tour.ratingsQuantity})`
            a.bt.w3-round-xxlarge.w3-btn.btn--green.w3-center(href=`/tour/${tour.slug}`,class='w3-hide-small w3-hide-medium',style='width:80%;font-size: 1.6rem;padding: 1.4rem 3rem;text-transform: uppercase;') Details
          a.bt.w3-btn.btn--green.w3-center(href=`/tour/${tour.slug}`,class='w3-hide-small w3-hide-large',style='width:100%;font-size: 1.6rem;padding: 1.4rem 3rem;text-transform: uppercase;margin-bottom:0%') Details
          .w3-container.w3-center(style='width:100%;padding:2%')
            a.bt.w3-round-xxlarge.w3-btn.btn--green.w3-center(href=`/tour/${tour.slug}`,class='w3-hide-large w3-hide-medium',style='font-size: 1.6rem;margin-left:10%;margin-right:10%;width:') Details